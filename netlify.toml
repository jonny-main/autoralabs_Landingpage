[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Use Netlify's Next.js runtime for server-side rendering and middleware
[[plugins]]
  package = "@netlify/plugin-nextjs"
